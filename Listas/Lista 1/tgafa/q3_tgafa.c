//3163 - Controlador de Vôo
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

typedef struct Node {

    Node *next;

} Node;

int main(){

    Node *head = NULL;
    int P;

    do{
        scanf("%d", &P);
        if(P == 0) break;
        


    }while(1);

    return 0;
}